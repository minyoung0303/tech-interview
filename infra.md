# 인프라 (Infrastructure)

> 기준: AWS(VPC, ALB/NLB, EKS, RDS, ElastiCache) / Docker / Kubernetes / Nginx / MySQL 8
> 대상 질문 9개. 백엔드 실무 면접에서 인프라 파트는 대체로 이 범위에서 나온다.
>
> **이 파일의 사용법**
> 1. 각 질문의 **0번 "30초 답변"만 먼저 외운다.** 9개 × 30초 = 5분이면 1차 방어선이 생긴다
> 2. 그다음 **꼬리질문 체크리스트**를 훑으면서 막히는 걸 본문에서 찾아 읽는다
> 3. 시각화가 필요하면 [visualize/](./visualize/index.html)로 간다

---

## 목차

| # | 질문 | 시각화 |
|---|---|---|
| 1 | 로드 밸런서 L4와 L7의 차이 | [scaling.html#s3](./visualize/scaling.html#s3) |
| 2 | Scale-up과 Scale-out의 차이 | [scaling.html#s1](./visualize/scaling.html#s1) |
| 3 | Web과 WAS를 나눠 구성하는 이유 | [scaling.html#s4](./visualize/scaling.html#s4) |
| 4 | Docker를 쓰는 이유와 VM과의 차이 | [container.html](./visualize/container.html) |
| 5 | VPC란 무엇이며 Public/Private Subnet을 분리하는 이유 | [vpc.html](./visualize/vpc.html) |
| 6 | 무중단 배포 방식 유형별 설명 | [deployment.html#s8](./visualize/deployment.html#s8) |
| 7 | Read Replica와 샤딩의 차이 | [dbscale.html](./visualize/dbscale.html) |
| 8 | Forward Proxy와 Reverse Proxy 차이 | [proxy.html](./visualize/proxy.html) |
| 9 | 세션 인증과 JWT를 Scale-out 관점에서 비교 | [scaling.html#s6](./visualize/scaling.html#s6) |

---

## 인프라 질문 공통 답변 전략

### 이 9개는 사실 **하나의 이야기**다

```
트래픽이 늘었다
  → Scale-up으로 버틴다 (Q2) … 곧 한계
  → Scale-out을 한다 (Q2)
      → 앞에 로드 밸런서가 필요하다 (Q1, Q8)
      → 서버가 상태를 갖고 있으면 안 된다 → 세션 문제 (Q9)
      → 정적 파일 처리를 앞단으로 뺀다 (Q3)
      → 서버를 똑같이 여러 개 찍어내야 한다 → 컨테이너 (Q4)
      → 그 서버들을 어디에 어떻게 배치하나 → VPC / Subnet (Q5)
      → 무중단으로 교체해야 한다 (Q6)
      → 앱은 늘렸는데 DB가 병목이다 → Read Replica → 샤딩 (Q7)
```

**면접에서 하나를 물었을 때 이 흐름의 앞뒤를 한 문장씩 붙이면 즉시 다르게 들린다.**

> "Scale-out을 하려면 서버가 stateless해야 하는데, 그래서 세션을 Redis로 빼거나 JWT를 쓰는 선택이 따라옵니다."

### 모르는 건 모른다고 한다

인프라는 꼬리질문으로 파고들기 가장 쉬운 영역이다.
직접 구축한 경험이 없으면 **"구축은 안 해봤고 개념과 실습 수준입니다"**로 선을 먼저 긋는다.
선을 그으면 면접관은 그 선 안에서 질문한다. 안 해본 걸 해봤다고 하면 두 번째 질문에서 무너진다.

---

# 1. 로드 밸런서 L4와 L7의 차이

## 0. 30초 답변

> "L4는 전송 계층에서 **IP와 포트**만 보고 분배합니다. HTTP 내용을 모르기 때문에 빠르지만 경로 기반 라우팅이 안 됩니다. AWS의 NLB입니다.
> L7은 애플리케이션 계층에서 **HTTP 메서드·경로·헤더·쿠키까지 읽고** 분배합니다. 그래서 `/api`는 API 서버로, `/admin`은 관리자 서버로 보내는 게 가능하고 TLS 종료와 WAF 연동도 여기서 합니다. AWS의 ALB입니다.
> 정리하면 **L4는 봉투만 보고 배달하고, L7은 편지를 열어 읽고 배달합니다.**"

## 1. 계층이 다르다는 게 실제로 뭘 의미하는가

| | L4 (전송 계층) | L7 (애플리케이션 계층) |
|---|---|---|
| 판단 근거 | IP, 포트, TCP/UDP | HTTP 메서드, 경로, 호스트, 헤더, 쿠키, 바디 |
| HTTP 이해 | 못 한다 | 한다 |
| 경로 기반 라우팅 | **불가능** | 가능 |
| TLS 종료 | 안 한다 (그대로 통과) | 한다 |
| TCP 연결 | 사실상 그대로 전달 | **클라이언트↔LB / LB↔서버 2개로 끊긴다** |
| 성능 | 빠르고 오버헤드 적다 | 상대적으로 무겁다 |
| 헬스 체크 | TCP 연결 성공 여부 | **HTTP 상태 코드 (200 확인)** |
| 클라이언트 IP | 그대로 보존 가능 | `X-Forwarded-For` 헤더로 전달 |
| AWS | **NLB** | **ALB** |
| 기타 제품 | LVS, HAProxy(tcp mode), F5 | Nginx, Envoy, Traefik, CloudFront, API Gateway |

### 가장 중요한 차이: 연결이 끊기는가

L7 로드 밸런서는 **요청을 자기가 다 받아서 해석한 뒤 새 연결로 백엔드에 보낸다.**
연결이 두 개로 쪼개진다는 사실에서 실무 특성이 전부 파생된다.

- 그래서 **TLS를 종료할 수 있다** (내용을 봐야 라우팅하니까 복호화가 필수다)
- 그래서 **클라이언트 IP가 사라진다** → `X-Forwarded-For`로 넘겨줘야 한다
- 그래서 **타임아웃이 두 겹**이 된다 (LB 타임아웃 / 앱 타임아웃)
- 그래서 **느린 클라이언트를 흡수**할 수 있다 (요청 버퍼링)
- 그래서 **재시도, 압축, 캐싱, 헤더 조작**이 가능하다

L4는 반대로 거의 그대로 흘려보내므로 위의 것들을 못 하지만 **훨씬 빠르고, 커넥션당 상태가 가볍고, TCP 이외의 프로토콜(임의의 바이너리 프로토콜)도 다룰 수 있다.**

## 2. 선택 기준 (실무 답변)

| 상황 | 선택 | 이유 |
|---|---|---|
| HTTP 경로/호스트 라우팅 필요 | **ALB (L7)** | L4는 원리적으로 불가능 |
| TLS 종료를 LB에서 하고 싶다 | **ALB** | ACM 인증서를 붙여 한 곳에서 관리 |
| WAF, 인증 연동(Cognito/OIDC) | **ALB** | L7에서만 가능 |
| **고정 IP**가 필요 (상대방이 IP 화이트리스트) | **NLB** | ALB는 IP가 바뀐다 |
| 초저지연, 초고성능 TCP | **NLB** | 오버헤드가 적다 |
| HTTP가 아닌 프로토콜 (MQTT, 커스텀 TCP, DB 프록시) | **NLB** | ALB는 HTTP/gRPC만 |
| 클라이언트 IP를 앱에서 그대로 받고 싶다 | **NLB** | 소스 IP 보존 가능 |

> **"둘 다 쓰는 구성"도 있다.** NLB → Nginx Ingress → 서비스 구조가 대표적이다.
> 고정 IP와 L4 성능은 NLB에서 얻고, 경로 라우팅은 뒤의 Nginx가 한다.
> 이걸 말하면 "구조를 그려본 사람"으로 읽힌다.

## 3. 오해 정리

**"L7이 상위 계층이니까 더 좋은 것"이 아니다.** 목적이 다르다.
L4는 성능과 범용성, L7은 기능과 제어. 트레이드오프다.

**"L4는 로드 밸런서, L7은 리버스 프록시"라는 구분도 정확하지 않다.**
L7 로드 밸런서는 리버스 프록시의 한 기능이다. 리버스 프록시가 더 큰 개념이다. → [proxy.md](./proxy.md)

## 4. 꼬리질문 체크리스트

- [ ] L4는 왜 경로 기반 라우팅을 못 하나? (→ HTTP를 파싱하지 않는다)
- [ ] L7에서 클라이언트 IP는 어떻게 알아내나? (→ XFF, 그리고 **위조 가능**하다)
- [ ] TLS 종료를 L4에서 할 수 있나? (→ 안 한다. 그래서 인증서 관리를 뒤로 미룬다)
- [ ] ALB와 NLB 중 고정 IP가 필요하면? (→ NLB)
- [ ] 로드 밸런싱 알고리즘은 뭐가 있나? (→ 라운드 로빈, 최소 연결, IP 해시, 가중치)
- [ ] 헬스 체크는 L4와 L7에서 어떻게 다른가? (→ TCP 연결 / HTTP 200)
- [ ] sticky session은 어떻게 구현되나? (→ L7은 쿠키, L4는 소스 IP 해시)
- [ ] 로드 밸런서가 단일 장애점이 되지 않나? (→ 관리형 LB는 내부적으로 다중화, 멀티 AZ)

## 5. 자주 하는 실수

- L4/L7만 외우고 **"그래서 실무에서 뭘 쓰는지"(ALB/NLB)를 못 말한다**
- L7의 이득을 "라우팅"만 말한다 → TLS 종료, WAF, 압축, 버퍼링, 재시도까지 한 세트
- 헬스 체크를 언급하지 않는다 → **헬스 체크 없는 로드 밸런싱은 무중단이 아니다**

---

# 2. Scale-up과 Scale-out의 차이

## 0. 30초 답변

> "Scale-up은 **서버 한 대의 사양을 올리는 수직 확장**이고, Scale-out은 **서버 대수를 늘리는 수평 확장**입니다.
> Scale-up은 코드를 안 고쳐도 되니까 간단하지만 물리적 한계가 있고 단일 장애점이 남고, 보통 재시작이 필요해서 다운타임이 생깁니다.
> Scale-out은 이론상 무한히 늘릴 수 있고 장애에 강하지만 **앞에 로드 밸런서가 필요하고 애플리케이션이 stateless해야** 합니다.
> 실무에서는 **애플리케이션 서버는 Scale-out, DB는 먼저 Scale-up 후 Read Replica**로 가는 게 일반적입니다. 쓰기 확장은 샤딩까지 가야 해서 비용이 크기 때문입니다."

**마지막 문장이 이 질문의 승부처다.** "앱은 out, DB는 up"이라는 실무 감각을 보여준다.

## 1. 비교

| | Scale-up (수직) | Scale-out (수평) |
|---|---|---|
| 방법 | CPU/메모리/디스크 증설, 인스턴스 타입 변경 | 서버 대수 증가 |
| 한계 | **물리적 상한이 있다** | 이론상 없다 |
| 비용 곡선 | 고사양일수록 **비선형으로 비싸진다** | 대체로 선형 |
| 다운타임 | 보통 **재시작 필요** | 없음 (한 대씩 추가) |
| 장애 내성 | 단일 장애점 그대로 | **한 대 죽어도 서비스 유지** |
| 필요 조건 | 없음 (코드 변경 없음) | 로드 밸런서 + **stateless** |
| 난이도 | 쉽다 | 분산 문제가 따라온다 |
| 축소 | 인스턴스 다운그레이드 (재시작) | 대수 감소 (즉시) |
| AWS | `t3.medium` → `m6i.2xlarge` | Auto Scaling Group, HPA |

## 2. Scale-out의 전제 조건: Stateless

**Scale-out에서 실제로 어려운 부분은 "서버를 늘리는 것"이 아니라 "서버가 갖고 있던 상태를 밖으로 빼는 것"이다.**

| 서버 안에 있으면 안 되는 것 | 어디로 빼는가 |
|---|---|
| 세션 | **Redis / ElastiCache** (또는 JWT) → Q9 |
| 업로드된 파일 | **S3** (로컬 디스크에 저장하면 다른 서버에서 못 읽는다) |
| 로컬 메모리 캐시 | Redis (또는 짧은 TTL로 불일치를 감수) |
| 스케줄러 / 배치 | 별도 워커, 또는 **분산 락으로 한 대만 실행** |
| 로그 파일 | stdout → CloudWatch / Loki (파일에 쓰면 파드가 죽을 때 사라진다) |
| WebSocket 연결 상태 | Redis Pub/Sub로 서버 간 브로드캐스트 |

**"Scale-out 해보셨나요?"에 경험이 없다면 이 표로 답한다.**
"서버를 늘리는 것 자체보다 상태를 밖으로 빼는 게 핵심이라고 이해하고 있습니다"는 개념 이해로 충분히 인정된다.

### 스케줄러 중복 실행 — 실제로 자주 터지는 사고

파드를 3개로 늘렸는데 매일 자정에 도는 정산 배치가 코드에 들어 있으면 **3번 실행된다.**
해결은 셋 중 하나다.

1. 배치를 별도 워커/K8s CronJob으로 분리한다 (정석)
2. Redis 분산 락으로 한 대만 잡게 한다
3. 처리 자체를 **멱등하게** 만든다 → [idempotency.md](./idempotency.md)

## 3. 어디까지 Scale-up으로 버티나

**"일단 Scale-up으로 버티는 게 나쁜 선택이 아니다."** 이걸 말할 수 있으면 좋다.

- 엔지니어 시간이 서버 비용보다 비싼 경우가 많다
- Scale-out은 분산 트랜잭션, 캐시 일관성, 디버깅 난이도 같은 **영구적인 복잡도**를 추가한다
- 특히 **DB는 쓰기 확장이 근본적으로 어렵다.** Scale-up → Read Replica → 캐시 → 샤딩 순서다 → Q7

그리고 순서상 **Scale-out보다 먼저 해야 하는 게 있다.**

```
1) 쿼리/인덱스 튜닝        ← 대부분 여기서 끝난다. 가장 싸다
2) 캐시 도입 (Redis, CDN)
3) N+1 제거, 커넥션 풀 조정
4) Scale-up
5) Scale-out
6) 아키텍처 변경 (읽기 분리, 비동기화, 샤딩)
```

> "확장 이야기가 나오면 먼저 병목이 어디인지 측정한 다음, 인덱스와 캐시로 해결되는지 보고, 그다음에 서버를 늘리는 순서로 접근합니다."
> **이 한 문장이 "무조건 Scale-out"이라고 답하는 지원자와 갈리는 지점이다.**

## 4. Auto Scaling과 함께 나오는 것들

- **ASG / HPA**: CPU, 메모리, 또는 커스텀 지표(요청 수, 큐 길이) 기준 자동 증감
- **Warm-up 시간**: 새 인스턴스가 트래픽을 받을 준비가 되는 데 걸리는 시간. 이게 길면 급격한 스파이크를 못 막는다
- **스케일 아웃했는데 DB 커넥션이 터진다**: 파드 10개 × 풀 20개 = 커넥션 200개. **DB `max_connections`를 넘긴다.** 실무에서 아주 흔한 사고다. RDS Proxy나 풀 크기 조정으로 대응한다
- **Cold start**: 컨테이너는 초 단위, VM은 분 단위. 그래서 스케일 아웃 속도가 다르다 → Q4

> **"스케일 아웃의 병목은 결국 DB로 내려간다."**
> 앱을 10배로 늘려도 DB가 하나면 DB가 새 병목이 된다. 이 인식이 Q7로 자연스럽게 이어진다.

## 5. 꼬리질문 체크리스트

- [ ] 왜 무조건 Scale-out이 정답이 아닌가? (→ 분산 복잡도, 비용, 먼저 할 게 있다)
- [ ] Scale-out을 하려면 애플리케이션이 어떤 조건을 만족해야 하나? (→ stateless)
- [ ] 세션을 서버 메모리에 두면 무슨 문제가 생기나? (→ sticky session 필요, 재시작 시 로그아웃) → Q9
- [ ] 파일 업로드는 어디에 저장하나? (→ S3. 로컬 디스크는 안 된다)
- [ ] 서버를 늘렸는데 배치가 여러 번 도는 문제는 어떻게 해결하나? (→ CronJob 분리, 분산 락, 멱등성)
- [ ] DB도 Scale-out 할 수 있나? (→ 읽기는 Replica, 쓰기는 샤딩. 난이도가 다르다) → Q7
- [ ] 파드를 늘렸는데 오히려 느려졌다면? (→ DB 커넥션 포화, 락 경합, 외부 API rate limit)
- [ ] Auto Scaling 기준 지표는 뭘로 잡나? (→ CPU만으로는 부족. 요청 수, p99 지연, 큐 길이)

## 6. 자주 하는 실수

- "Scale-up은 한계가 있어서 Scale-out이 좋습니다"로 끝낸다 → **트레이드오프를 말해야 한다**
- Stateless 조건을 언급하지 않는다 → 이게 실무의 핵심 난관이다
- DB를 앱과 똑같이 취급한다 → **DB의 쓰기 확장은 완전히 다른 문제다**

---

# 3. Web과 WAS를 나눠 구성하는 이유

## 0. 30초 답변

> "역할이 다르기 때문입니다. Web 서버는 **정적 자원 응답과 리버스 프록시** 역할이고, WAS는 **동적 요청의 비즈니스 로직 처리**입니다.
> 나누면 첫째, 정적 파일 처리를 Web이 흡수해서 **WAS의 스레드/워커를 비즈니스 로직에만 쓸 수 있고**,
> 둘째, WAS를 **사설망에 두고 Web만 노출**해서 보안 경계를 만들 수 있고,
> 셋째, Web이 앞에 있으니 **로드 밸런싱·헬스 체크·무중단 배포·TLS 종료**가 가능해집니다.
> 요즘 클라우드 환경에서는 Nginx 대신 **ALB + CloudFront가 그 역할을 대신**하는 경우도 많습니다."

**마지막 문장이 "교과서만 읽은 사람"과 갈리는 지점이다.**

## 1. 역할 구분

```
[클라이언트]
     │  HTTPS
     ▼
[Web Server]  Nginx / Apache          ← 정적 파일, TLS 종료, 압축, 캐시, 라우팅
     │  HTTP (사설망)
     ▼
[WAS]  Tomcat / Go 앱 / Node / Django  ← 비즈니스 로직, 세션, 트랜잭션
     │
     ▼
[DB]  MySQL                            ← 데이터
```

| | Web Server | WAS (Application Server) |
|---|---|---|
| 주 역할 | 정적 콘텐츠 응답, 리버스 프록시 | 동적 요청 처리, 비즈니스 로직 |
| 처리 대상 | HTML, CSS, JS, 이미지 | API, DB 조회, 계산, 트랜잭션 |
| 대표 제품 | Nginx, Apache | Tomcat, Jetty, Go 앱, Gunicorn, Node |
| 위치 | Public 경계 (DMZ) | **Private Subnet** |
| 리소스 특성 | I/O 위주, 가볍다 | CPU/메모리 위주, 무겁다 |

## 2. 나누는 이유 6가지

### (1) 정적 자원 처리를 오프로드한다

이미지 요청이 WAS까지 올라가면 **비즈니스 로직용 워커가 이미지 전송에 묶인다.**
Nginx는 이걸 훨씬 적은 자원으로 처리한다. WAS 워커 수는 유한하고 비싸다.

### (2) 보안 경계를 만든다

WAS를 Private Subnet에 두면 **인터넷에서 직접 접근이 불가능하다.**
Web/LB만 노출되므로 공격 표면이 줄어든다. → Q5와 직결된다.

### (3) 여러 WAS 앞단을 하나로 묶는다

로드 밸런싱, 헬스 체크, 커넥션 드레이닝. **무중단 배포의 물리적 전제다.** → Q6

### (4) 관심사가 분리된다

TLS 인증서, gzip/brotli 압축, 캐시 헤더, 요청 크기 제한, Rate Limit, WAF 같은
**"어느 애플리케이션에나 필요한 것"을 앱 코드 밖으로 뺀다.**

### (5) 느린 클라이언트를 흡수한다 (잘 알려지지 않은 실질적 이득)

Nginx가 느린 모바일 클라이언트의 요청을 **다 받은 뒤** 한 번에 백엔드로 넘긴다.
백엔드 워커가 느린 네트워크에 붙잡혀 있지 않게 된다. 응답도 마찬가지로 버퍼링한다.

### (6) 장애 격리와 점검 페이지

WAS가 전부 죽어도 Web이 살아 있으면 **502 대신 점검 페이지**를 보여줄 수 있다.

## 3. 그런데 요즘은 (중요한 반전)

**"Nginx 없이 앱만 띄우면 안 되나요?"** 이 꼬리질문이 반드시 온다.

- **정적 파일은 이미 CDN/S3로 간다.** Nginx가 정적 파일을 서빙할 일이 줄었다
- **TLS 종료와 L7 라우팅은 ALB가 한다.** Nginx의 대표 역할을 관리형 LB가 흡수했다
- **컨테이너 안에는 앱 프로세스만** 두는 게 원칙이다 (1 컨테이너 1 프로세스)
- Kubernetes에서는 **Nginx Ingress Controller가 클러스터 입구**에서 Web 서버 역할을 한다

즉 **"Web/WAS 분리"라는 개념은 유지되지만, Web 계층의 구현체가 Nginx에서 ALB·CloudFront·Ingress로 옮겨간 것이다.**

```
[예전]   Nginx (EC2)  →  Tomcat (같은 EC2 또는 옆 EC2)
[요즘]   CloudFront → ALB → (Nginx Ingress) → 앱 컨테이너
```

> "역할 분리는 그대로지만 구현이 관리형 서비스로 옮겨갔습니다. 저희가 EKS를 쓴다면 Nginx는 Ingress Controller 자리에 있게 됩니다."
> **이렇게 답하면 개념과 현실을 둘 다 아는 것으로 읽힌다.**

## 4. 꼬리질문 체크리스트

- [ ] Web 서버와 WAS의 차이를 한 줄로? (→ 정적/프록시 vs 동적/로직)
- [ ] Nginx 없이 앱만 띄우면 안 되나? (→ 된다. 다만 위 6가지를 다른 것이 대신해야 한다)
- [ ] Apache와 Nginx의 차이는? (→ 프로세스/스레드 기반 vs **이벤트 기반 비동기**. C10K 문제)
- [ ] WAS를 여러 대 두면 세션은 어떻게 하나? (→ Redis 또는 JWT) → Q9
- [ ] 3-tier 아키텍처가 뭔가? (→ Web / App / DB 계층 분리. Subnet 설계와 대응된다) → Q5
- [ ] 정적 파일은 어디에 두는 게 좋나? (→ S3 + CloudFront)
- [ ] Nginx의 `client_max_body_size` 기본값은? (→ **1MB**. 업로드 붙이고 413 나는 전형적 원인)

## 5. 자주 하는 실수

- "Web은 정적, WAS는 동적"에서 멈춘다 → **보안 경계와 로드 밸런싱**이 실무에서 더 큰 이유다
- 클라우드 현실(ALB, CDN, Ingress)과 연결하지 못한다
- 3-tier 아키텍처 / Subnet 분리와 이어서 말하지 못한다 → 같은 이야기다

---

# 4. Docker를 사용하는 이유와 VM과의 차이점

## 0. 30초 답변

> "가장 큰 차이는 **커널을 공유하는지**입니다.
> VM은 하이퍼바이저 위에 **게스트 OS 전체와 별도 커널**을 올립니다. 그래서 GB 단위이고 부팅이 분 단위지만 격리가 강합니다.
> 컨테이너는 **호스트 커널을 공유**하고 리눅스의 **namespace로 격리, cgroup으로 자원 제한**을 겁니다. 프로세스 수준이라 MB 단위이고 시작이 초 단위지만, 커널을 공유하니 격리 수준은 VM보다 약합니다.
> Docker를 쓰는 이유는 **환경 일관성**이 첫째입니다. 라이브러리와 런타임까지 이미지에 담아서 '내 로컬에서는 되는데'를 없앱니다. 여기에 배포 단위 표준화, 태그 기반 즉시 롤백, 빠른 Scale-out이 따라옵니다."

## 1. 구조 비교

```
[ VM ]                              [ 컨테이너 ]
┌──────────┬──────────┐             ┌──────────┬──────────┐
│  App A   │  App B   │             │  App A   │  App B   │
├──────────┼──────────┤             ├──────────┴──────────┤
│ 게스트OS │ 게스트OS │  ← 커널 2개 │  Docker Engine       │
├──────────┴──────────┤             ├─────────────────────┤
│    하이퍼바이저      │             │   호스트 OS (커널 1개) │  ← 공유
├─────────────────────┤             ├─────────────────────┤
│      호스트 OS       │             │      하드웨어        │
├─────────────────────┤             └─────────────────────┘
│      하드웨어        │
└─────────────────────┘
```

| | VM | 컨테이너 |
|---|---|---|
| 격리 단위 | 하드웨어 가상화 | **프로세스 격리** (namespace) |
| 커널 | 게스트마다 별도 | **호스트와 공유** |
| 크기 | GB 단위 | MB 단위 (Go 앱 alpine 20MB) |
| 시작 시간 | 수십 초 ~ 분 | **밀리초 ~ 초** |
| 밀도 | 호스트당 수 개 ~ 수십 개 | 수백 개 |
| 격리 강도 | **강하다** | 상대적으로 약하다 |
| OS 선택 | 호스트와 다른 OS 가능 (Windows on Linux) | **호스트 커널과 같은 계열만** |
| 대표 | EC2, VMware, KVM, VirtualBox | Docker, containerd, Podman |

### 컨테이너를 만드는 리눅스 기능 3개 (이걸 말하면 확실히 다르다)

| 기능 | 역할 |
|---|---|
| **namespace** | 격리. PID, 네트워크, 마운트, 호스트명, 사용자를 분리해서 "자기만의 세계"를 보게 한다 |
| **cgroup** | 자원 제한. CPU, 메모리, I/O 사용량 상한 |
| **UnionFS / OverlayFS** | 레이어. 읽기 전용 이미지 레이어 + 쓰기 가능 레이어(Copy-on-Write) |

> "컨테이너는 가상 머신이 아니라 **격리된 프로세스**입니다. 호스트에서 `ps`를 치면 그냥 프로세스로 보입니다."
> 이 한 문장이 개념 이해를 증명한다.

## 2. Docker를 쓰는 이유 (질문의 절반)

### (1) 환경 일관성 — 첫 번째 이유

OS 라이브러리, 런타임 버전, 시스템 패키지, 타임존까지 이미지에 고정된다.
**로컬 / CI / 스테이징 / 프로덕션이 같은 이미지로 돈다.**
"내 로컬에서는 되는데요"가 구조적으로 사라진다.

### (2) 배포 단위의 표준화

언어가 Go든 Python이든 Java든 **배포 단위가 "이미지" 하나로 통일된다.**
CI/CD 파이프라인, 오케스트레이션, 모니터링을 언어와 무관하게 같은 방식으로 짤 수 있다.

### (3) 즉시 롤백

이미지 태그가 커밋 SHA면 **이전 태그로 다시 배포하는 것이 롤백이다.**
그래서 `latest` 태그를 쓰면 안 된다 — 되돌릴 대상이 사라진다. → [deployment.md](./deployment.md)

### (4) 빠른 Scale-out

시작이 초 단위라서 트래픽 스파이크에 대응할 수 있다. VM은 분 단위라 늦다. → Q2

### (5) 오케스트레이션의 전제

Kubernetes의 셀프 힐링, 오토 스케일링, 롤링 업데이트는 **컨테이너가 가볍고 대체 가능(disposable)하다는 전제** 위에서 동작한다.

### (6) 격리와 이식성

한 호스트에서 서로 다른 버전(MySQL 5.7 + 8.0)을 충돌 없이 돌릴 수 있다.

## 3. 이미지와 컨테이너의 관계

```
이미지 (읽기 전용, 불변)         컨테이너 (실행 인스턴스)
┌──────────────────┐             ┌──────────────────┐
│ 앱 바이너리 레이어 │             │  쓰기 가능 레이어  │ ← 여기 쓴 건 컨테이너와 함께 사라진다
├──────────────────┤             ├──────────────────┤
│ 의존성 레이어      │   ──────▶   │  (아래는 이미지 레이어를 공유) │
├──────────────────┤             │                  │
│ 베이스 OS 레이어   │             │                  │
└──────────────────┘             └──────────────────┘
```

- **이미지는 클래스, 컨테이너는 인스턴스**로 비유하면 이해가 빠르다
- 레이어는 **여러 컨테이너가 공유**한다. 그래서 같은 이미지 100개를 띄워도 디스크가 100배 들지 않는다
- 컨테이너 안에 쓴 데이터는 **컨테이너가 사라지면 없어진다.** 그래서 **볼륨(Volume)** 이나 외부 스토리지(S3, RDS)를 쓴다

### 레이어 캐시와 Dockerfile 순서

```dockerfile
# 좋은 순서: 자주 바뀌는 것을 뒤에
COPY go.mod go.sum ./
RUN go mod download        # ← 의존성이 안 바뀌면 이 레이어 캐시가 재사용된다
COPY . .                   # ← 소스는 자주 바뀌므로 뒤에
RUN go build ...
```

순서를 뒤집으면(`COPY . .`을 먼저) **소스 한 줄만 바꿔도 의존성을 매번 다시 내려받는다.**
빌드가 10초에서 3분으로 늘어난다. 실무에서 즉시 체감되는 포인트다.

## 4. 실무 함정 (여기서 깊이가 갈린다)

| 함정 | 내용 |
|---|---|
| **컨테이너는 stateless** | 로컬 디스크에 파일 저장 금지. S3나 볼륨을 쓴다 |
| **로그는 stdout으로** | 파일에 쓰면 컨테이너가 죽을 때 사라진다. 수집기가 stdout을 읽는다 |
| **1 컨테이너 1 프로세스** | 앱과 Nginx를 한 컨테이너에 넣지 않는다. 사이드카나 Ingress로 분리 |
| **root로 실행 금지** | `USER appuser`. 컨테이너 탈출 시 피해를 줄인다 |
| **`latest` 태그 금지** | 롤백 대상이 사라지고 무엇이 떴는지 알 수 없다 |
| **멀티 스테이지 빌드** | 800MB → 20MB. 최종 이미지에 컴파일러와 소스를 남기지 않는다 |
| **PID 1 시그널 문제** | `ENTRYPOINT`를 **exec 형식**(`["/app/server"]`)으로. shell 형식이면 SIGTERM이 앱에 안 간다 → graceful shutdown 실패 |
| **타임존 / CA 인증서** | `alpine`, `scratch`에는 없다. `tzdata`, `ca-certificates` 설치 |
| **리소스 limit과 런타임** | cgroup limit을 런타임이 못 읽으면 호스트 전체 사양으로 오인한다. Go는 `GOMAXPROCS`, JVM은 `MaxRAMPercentage` 확인 |

> **PID 1 시그널 문제는 강력한 카드다.**
> "무중단 배포인데 502가 났다"의 원인으로 이어지고, graceful shutdown → preStop → 커넥션 드레이닝까지
> 하나의 이야기로 연결된다. → [deployment.md](./deployment.md)

## 5. "그럼 VM은 이제 안 쓰나요?"

쓴다. 그리고 **둘은 대체 관계가 아니라 계층 관계로 함께 쓰인다.**

```
EC2 인스턴스 (VM)  ← EKS 워커 노드
   └─ 컨테이너 여러 개 (파드)
```

**컨테이너는 대개 VM 위에서 돈다.** 이걸 아는 게 중요하다.

| VM이 필요한 경우 | 이유 |
|---|---|
| 강한 격리가 필요한 멀티테넌시 | 커널 공유가 위험하다. 남의 코드를 돌리는 경우 |
| 다른 OS 커널이 필요 | Windows 워크로드, 특정 커널 모듈 |
| 커널 파라미터를 직접 만져야 함 | 컨테이너는 호스트 커널을 공유하므로 제약 |
| 규제/컴플라이언스 요구 | 물리적 분리 수준의 격리 증명 |

> **가산점 한 줄**: AWS Lambda와 Fargate는 **Firecracker**라는 마이크로 VM을 쓴다.
> 컨테이너의 속도와 VM의 격리를 동시에 얻으려는 접근이고, 같은 계열로 gVisor, Kata Containers가 있다.

> **또 하나**: macOS나 Windows의 Docker Desktop은 사실 **내부에 리눅스 VM을 띄우고 그 안에서 컨테이너를 돌린다.**
> 컨테이너가 리눅스 커널 기능이기 때문이다. "왜 맥에서 Docker가 느린가"의 답도 이것이다.

## 6. 꼬리질문 체크리스트

- [ ] 컨테이너와 VM의 가장 큰 차이는? (→ **커널 공유 여부**)
- [ ] 컨테이너 격리는 무엇으로 구현되나? (→ namespace + cgroup + UnionFS)
- [ ] 컨테이너가 VM보다 격리가 약하다는 게 무슨 뜻인가? (→ 커널 취약점이 호스트 전체에 영향)
- [ ] 이미지와 컨테이너의 차이는? (→ 불변 레이어 vs 쓰기 레이어를 얹은 실행 인스턴스)
- [ ] 컨테이너 안의 데이터는 어떻게 되나? (→ 사라진다. 볼륨/외부 스토리지)
- [ ] 왜 `latest` 태그를 쓰면 안 되나? (→ 롤백 불가, 재현 불가)
- [ ] 멀티 스테이지 빌드는 왜 쓰나? (→ 크기, 보안)
- [ ] Dockerfile 레이어 순서는 왜 중요한가? (→ 캐시 재사용)
- [ ] 컨테이너에 SSH를 넣어도 되나? (→ 안 된다. `kubectl exec`를 쓴다)
- [ ] `ENTRYPOINT`와 `CMD`의 차이는? exec 형식과 shell 형식의 차이는? (→ 시그널 전달)
- [ ] Docker와 Kubernetes의 관계는? (→ 컨테이너 런타임 vs 오케스트레이터)
- [ ] 컨테이너가 VM 위에서 도는 건가? (→ 클라우드에서는 대개 그렇다)

## 7. 자주 하는 실수

- "컨테이너는 가벼운 VM"이라고 설명한다 → **격리된 프로세스**다. 이 차이가 질문의 핵심이다
- "Docker를 쓰면 편해요"로 끝낸다 → **환경 일관성**이라는 단어를 반드시 쓴다
- 컨테이너가 VM을 대체했다고 말한다 → 함께 쓰인다
- 격리가 약하다는 단점을 모른다 → 트레이드오프를 아는 게 신뢰를 만든다

---

# 5. VPC란 무엇이며, Public Subnet과 Private Subnet을 분리하는 이유

## 0. 30초 답변

> "VPC는 AWS 안에 만드는 **논리적으로 격리된 사설 네트워크**입니다. CIDR 블록으로 IP 대역을 정하고, 그 안을 AZ 단위로 서브넷으로 나눕니다.
> Public Subnet과 Private Subnet의 차이는 물리적인 게 아니고 **라우팅 테이블에 인터넷 게이트웨이(IGW)로 향하는 경로가 있는지**입니다. 있으면 Public, 없으면 Private입니다.
> 분리하는 이유는 **공격 표면 최소화**입니다. 인터넷에 노출될 필요가 있는 ALB와 NAT Gateway만 Public에 두고, 앱 서버와 DB는 Private에 둬서 **인터넷에서 직접 도달할 수 없게** 만듭니다.
> Private에서 외부로 나가는 통신은 Public Subnet에 있는 **NAT Gateway를 경유**시켜서 '나갈 수는 있지만 들어올 수는 없는' 상태를 만듭니다."

**"라우팅 테이블에 IGW 경로가 있는지"가 정의라는 걸 말하면 확실히 다르다.**
대부분은 "인터넷에 연결되면 Public"이라고만 답한다.

## 1. VPC 구성 요소

| 요소 | 역할 |
|---|---|
| **VPC** | 격리된 가상 네트워크. CIDR 지정 (예: `10.0.0.0/16` = 65,536개 IP) |
| **Subnet** | VPC를 쪼갠 단위. **하나의 AZ에 종속된다** (AZ를 넘을 수 없다) |
| **Route Table** | 목적지 CIDR별로 어디로 보낼지 결정. **Public/Private을 가르는 실체** |
| **Internet Gateway (IGW)** | VPC ↔ 인터넷 양방향 통로. VPC당 1개 |
| **NAT Gateway** | Private → 외부 **단방향** 아웃바운드. Public Subnet에 위치 |
| **Security Group (SG)** | 인스턴스(ENI) 단위 방화벽. **stateful**, allow만 |
| **Network ACL (NACL)** | 서브넷 단위 방화벽. **stateless**, allow + deny |
| **VPC Endpoint** | S3/DynamoDB 등 AWS 서비스로 **인터넷을 안 거치고** 접근 |

## 2. 전체 그림

```
                          인터넷
                             │
                     ┌───────▼────────┐
                     │ Internet Gateway│
                     └───────┬────────┘
   ┌─────────────────────────┼──────────────────────────┐
   │ VPC  10.0.0.0/16        │                          │
   │                         │                          │
   │  ┌─────────── Public Subnet (AZ-a / AZ-c) ───────┐ │
   │  │  10.0.1.0/24, 10.0.2.0/24                     │ │
   │  │   ALB          NAT Gateway      (Bastion)     │ │
   │  │   0.0.0.0/0 → IGW  ← 라우팅 테이블에 이게 있다  │ │
   │  └───────┬───────────────────┬───────────────────┘ │
   │          │ HTTP              │ 아웃바운드만          │
   │  ┌───────▼───────── Private Subnet (App) ────────┐ │
   │  │  10.0.11.0/24, 10.0.12.0/24                   │ │
   │  │   EKS 워커 노드 / 앱 컨테이너                    │ │
   │  │   0.0.0.0/0 → NAT Gateway   ← IGW 경로가 없다   │ │
   │  └───────┬───────────────────────────────────────┘ │
   │          │ 3306                                     │
   │  ┌───────▼───────── Private Subnet (DB) ─────────┐ │
   │  │  10.0.21.0/24, 10.0.22.0/24                   │ │
   │  │   RDS (Multi-AZ)   ElastiCache                 │ │
   │  │   외부로 나가는 경로 자체가 없다 (완전 격리)        │ │
   │  └───────────────────────────────────────────────┘ │
   └────────────────────────────────────────────────────┘
```

**Q3의 Web/WAS/DB 3-tier가 그대로 서브넷 3계층으로 대응된다.** 같은 이야기다.

## 3. Public / Private의 정의는 라우팅 테이블이다

```
[Public Subnet 라우팅 테이블]
  10.0.0.0/16  →  local          (VPC 내부 통신)
  0.0.0.0/0    →  igw-xxxx       ← 이 한 줄이 Public으로 만든다

[Private Subnet 라우팅 테이블]
  10.0.0.0/16  →  local
  0.0.0.0/0    →  nat-xxxx       ← IGW가 아니라 NAT를 가리킨다
```

### 여기서 나오는 필수 꼬리질문 3개

**(1) "Public Subnet에 뒀는데 인터넷에서 접근이 안 됩니다. 왜죠?"**

Public Subnet이어도 **인스턴스에 Public IP가 없으면** 인바운드가 불가능하다.
Public Subnet + Public IP(또는 EIP) + SG 허용, 이 세 가지가 다 있어야 한다.

**(2) "왜 NAT Gateway가 Public Subnet에 있어야 하나요?"**

NAT 자신이 인터넷으로 나갈 수 있어야 하기 때문이다.
NAT를 Private Subnet에 두면 NAT도 인터넷에 못 나가서 아무 의미가 없다.

**(3) "Private Subnet의 서버가 외부 API를 호출할 수 있나요?"**

NAT Gateway가 있으면 **나가는 건 된다.** 들어오는 건 안 된다.
NAT는 **아웃바운드로 시작된 연결의 응답만** 돌려준다. 이게 "나갈 수는 있지만 들어올 수는 없다"의 구현이다.

## 4. 분리하는 이유

### (1) 공격 표면 최소화 — 가장 중요한 답

DB가 Private Subnet에 있으면 **인터넷에서 도달할 경로 자체가 없다.**
SG 설정 실수 하나로 DB가 열리는 사고를 **네트워크 레벨에서 미리 막는 것**이다.

> 보안 설정은 실수한다는 전제로 설계한다. 그래서 **다층 방어(defense in depth)** 다.
> SG(1층) + NACL(2층) + 라우팅 없음(3층). 이 단어를 쓰면 좋다.

### (2) 침해 시 확산(lateral movement) 차단

ALB가 뚫려도 그 다음 계층으로 가려면 SG와 라우팅을 또 통과해야 한다.

### (3) 계층별 접근 제어를 명시적으로 만든다

```
ALB SG        : 인터넷(0.0.0.0/0) → 443 허용
App SG        : 소스를 "ALB SG"로 지정 → 8080 허용   ← IP가 아니라 SG를 소스로 지정
DB SG         : 소스를 "App SG"로 지정 → 3306 허용
```

**SG를 소스로 지정하는 방식**을 알고 있으면 실무 감각으로 인정된다.
IP 대역으로 쓰면 오토 스케일링으로 IP가 바뀔 때 관리가 안 된다.

### (4) 비용과 트래픽 통제

아웃바운드가 NAT 한 곳으로 모이므로 **어디로 나가는지 관측하고 제어할 수 있다.**

## 5. Security Group vs Network ACL (거의 항상 이어서 물어본다)

| | Security Group | Network ACL |
|---|---|---|
| 적용 대상 | 인스턴스 / ENI | **서브넷** |
| 상태 | **Stateful** — 인바운드 허용하면 응답 아웃바운드는 자동 허용 | **Stateless** — 인바운드와 아웃바운드를 각각 열어야 한다 |
| 규칙 | **allow만** | allow + **deny 가능** |
| 평가 | 모든 규칙을 합쳐서 판단 | **번호 순서대로**, 먼저 매칭되면 종료 |
| 기본값 | 인바운드 전부 거부 / 아웃바운드 전부 허용 | 기본 NACL은 전부 허용 |
| 주 용도 | 일상적인 접근 제어 | 특정 IP 차단, 서브넷 단위 광역 통제 |

> **실무에서는 SG를 주로 쓰고 NACL은 기본값으로 두는 경우가 많다.**
> "NACL은 stateless라서 응답 포트(임시 포트 1024–65535)까지 열어야 해서 관리가 번거롭습니다"까지 말하면 좋다.

## 6. 실무에서 챙기는 것들

| 항목 | 내용 |
|---|---|
| **Multi-AZ** | 서브넷은 AZ에 종속된다. ALB는 **최소 2개 AZ**, RDS 서브넷 그룹도 2개 AZ 이상 필요 |
| **NAT는 AZ마다** | NAT를 한 AZ에만 두면 그 AZ 장애 시 다른 AZ의 아웃바운드가 끊긴다. 비용 vs 가용성 트레이드오프 |
| **NAT 비용** | 시간당 요금 + **데이터 처리 요금**. ECR 이미지 pull, S3 전송이 여기로 나가면 비싸진다 |
| **VPC Endpoint** | S3는 **Gateway Endpoint(무료)**, ECR·SSM 등은 Interface Endpoint. NAT 비용을 줄이고 트래픽을 AWS 내부로 유지 |
| **Bastion 대신 SSM** | **Session Manager**를 쓰면 22번 포트를 열지 않고 Private 인스턴스에 접속할 수 있다. 요즘 정석 |
| **CIDR 설계** | 나중에 늘리기 어렵다. `/16`으로 넉넉히 잡고 서브넷은 `/24` 단위. **EKS는 파드마다 VPC IP를 소비**하므로 서브넷을 크게 잡아야 한다 |
| **CIDR 중복 금지** | VPC 피어링이나 온프레미스 VPN을 붙일 때 대역이 겹치면 연결이 불가능하다 |
| **VPC 피어링 / Transit Gateway** | VPC 간 연결. 피어링은 **전이적 라우팅이 안 된다**(A-B, B-C가 있어도 A-C는 불가) |

> **"EKS는 파드마다 IP를 쓴다"**는 실제 운영에서 자주 터지는 문제다.
> 서브넷 IP가 고갈되면 파드가 `Pending`으로 뜨지 않는다. 이걸 알면 눈에 띈다.

## 7. 꼬리질문 체크리스트

- [ ] Public Subnet과 Private Subnet의 차이는 무엇으로 결정되나? (→ **라우팅 테이블의 IGW 경로**)
- [ ] Private Subnet에서 인터넷으로 나갈 수 있나? (→ NAT Gateway 경유하면 가능. 인바운드는 불가)
- [ ] NAT Gateway는 왜 Public Subnet에 두나?
- [ ] Public Subnet인데 접속이 안 되는 이유는? (→ Public IP 없음 / SG 차단)
- [ ] SG와 NACL의 차이는? (→ stateful/stateless, allow만/deny 가능, ENI/서브넷)
- [ ] DB를 Private에 뒀는데 로컬에서 접속하려면? (→ Bastion, SSM 포트 포워딩, VPN)
- [ ] 서브넷은 AZ를 걸칠 수 있나? (→ 못 한다. 그래서 AZ마다 서브넷을 만든다)
- [ ] IGW와 NAT Gateway의 차이는? (→ 양방향 / 아웃바운드 단방향)
- [ ] S3에 접근하는데 NAT 비용을 줄이려면? (→ **S3 Gateway Endpoint**)
- [ ] 3-tier 아키텍처와 서브넷 설계는 어떻게 대응되나?

## 8. 자주 하는 실수

- "Public은 인터넷 되고 Private은 안 된다"로만 말한다 → **라우팅 테이블**이 정의다
- 분리 이유를 "보안 때문"이라고만 한다 → **공격 표면 최소화 / 침해 확산 차단 / 다층 방어**로 구체화한다
- NAT Gateway를 언급하지 않는다 → Private 서버가 패키지 설치나 외부 API 호출을 어떻게 하는지 설명이 안 된다
- SG와 NACL 차이를 모른다 → 거의 항상 이어서 물어본다

---

# 6. 무중단 배포 방식 유형별 설명

## 0. 30초 답변

> "크게 네 가지입니다.
> **Recreate**는 전부 내리고 새로 올리는 방식이라 다운타임이 생깁니다. 무중단이 아닙니다.
> **Rolling Update**는 일부씩 순차 교체합니다. 추가 리소스가 적고 K8s 기본값이지만 **두 버전이 동시에 존재**합니다.
> **Blue/Green**은 새 환경 전체를 띄우고 트래픽을 한 번에 스위치합니다. **롤백이 즉시** 가능하지만 리소스가 2배 듭니다.
> **Canary**는 소수 트래픽만 신 버전으로 보내 지표를 확인하고 점진적으로 확대합니다. 위험이 가장 작지만 구성이 복잡하고 관측 도구가 필요합니다.
> 그리고 어떤 방식을 쓰든 **헬스 체크, graceful shutdown, 하위 호환 스키마**가 없으면 무중단이 되지 않습니다."

**마지막 문장이 이 질문의 핵심이다.** 방식 이름 4개는 누구나 외운다.

## 1. 네 가지 비교

| 전략 | 방식 | 다운타임 | 롤백 속도 | 추가 리소스 | 두 버전 공존 | 복잡도 |
|---|---|---|---|---|---|---|
| **Recreate** | 다 내리고 새로 올림 | **있다** | 재배포 필요 | 없음 | 없음 | 최저 |
| **Rolling Update** | 일부씩 순차 교체 | 없음 | 순차 롤백 (느림) | 적음 (`maxSurge`) | **있다** | 낮음 |
| **Blue/Green** | 새 환경 전체 → 스위치 | 없음 | **즉시** (다시 스위치) | **2배** | 전환 순간만 | 중간 |
| **Canary** | 소수 → 지표 확인 → 확대 | 없음 | 빠름 | 조금 | **있다 (의도적)** | 높음 |

### 그림으로

```
Recreate         [v1 v1 v1] → [      ] → [v2 v2 v2]      ← 가운데가 다운타임
Rolling          [v1 v1 v1] → [v2 v1 v1] → [v2 v2 v1] → [v2 v2 v2]
Blue/Green       [v1 v1 v1] + [v2 v2 v2] 동시 존재 → LB 스위치 → v1 폐기
Canary           [v1 v1 v1] + [v2] 5% → 지표 OK → 25% → 50% → 100%
```

## 2. 각 방식의 실무 포인트

### Rolling Update — K8s 기본값

```yaml
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 1          # 원하는 수보다 최대 1개 더 띄운다
    maxUnavailable: 0    # 사용 불가 파드를 0개로 유지 ← 무중단의 핵심
```

`maxUnavailable: 0`이 핵심이다. 기본값(25%)이면 **일시적으로 처리 용량이 줄어든다.**
트래픽이 많은 시간대에는 남은 파드가 부하를 못 견뎌 연쇄 장애가 날 수 있다.

**단점**: 신·구 버전이 동시에 돈다. 그래서
- API 응답 형식이 바뀌면 클라이언트가 요청마다 다른 응답을 받을 수 있다
- **DB 스키마가 하위 호환이 아니면 구 버전 파드가 500을 뿜는다** → 확장-수축 패턴 필요

### Blue/Green

- **가장 큰 장점은 롤백 속도다.** 트래픽만 되돌리면 끝난다
- 스위치 전에 **신 환경에 스모크 테스트를 실행**할 수 있다 (Rolling에서는 불가능)
- **함정 1: DB는 하나다.** Blue와 Green이 같은 DB를 본다. 그래서 스키마 문제는 여전히 남는다
- **함정 2: 커넥션 드레이닝.** 스위치 직후 Blue를 바로 죽이면 처리 중인 요청이 끊긴다
- **함정 3: 캐시/커넥션 풀 워밍업.** 갓 뜬 Green으로 100% 트래픽을 한 번에 보내면 첫 요청들이 느리다
- AWS에서는 ALB **가중치 기반 타겟 그룹**이나 Route 53 레코드 전환으로 구현한다

### Canary

- 트래픽 분할은 LB 가중치(ALB 타겟 그룹 비율), 서비스 메시(Istio), 또는 파드 수 비율로 구현
- **지표 기준 자동 승격/롤백**이 본질이다. 에러율·p99 지연을 보고 판단한다
- 도구: **Argo Rollouts, Flagger**. 이름을 아는 것만으로도 도움이 된다
- 사용자별 일관성이 필요하면 세션 어피니티나 사용자 ID 해시로 고정한다

### A/B 테스트와 구분하기 (좋은 가산점)

**Canary는 배포 안전장치, A/B 테스트는 제품 실험이다.** 목적이 다르다.
Canary는 "문제 없나?"를 보고, A/B는 "어느 쪽이 전환율이 높나?"를 본다.

### Feature Flag — 배포와 릴리스를 분리한다

코드는 이미 배포해두고 **기능만 플래그로 켠다.**
문제가 생기면 재배포 없이 플래그를 끄면 된다. **롤백이 초 단위가 된다.**

> "무중단 배포의 최종 형태는 배포(deploy)와 릴리스(release)를 분리하는 것이라고 생각합니다."
> 이 문장을 쓸 수 있으면 좋다.

## 3. 방식보다 중요한 것: 무중단의 전제 조건

**여기가 이 질문의 진짜 승부처다.** 방식만 나열하면 평범한 답변이다.

| 전제 | 없으면 어떻게 되나 |
|---|---|
| **readinessProbe** | 아직 준비 안 된 파드로 트래픽이 가서 **502/503** |
| **Graceful Shutdown** | SIGTERM 받자마자 죽어서 **처리 중인 요청이 끊긴다** |
| **preStop + sleep** | 엔드포인트 전파(LB 등록 해제)가 끝나기 전에 파드가 죽어서 **502** |
| **커넥션 드레이닝** | 종료 중인 서버로 새 요청이 계속 들어온다 |
| **하위 호환 스키마** | 구 버전 파드가 없는 컬럼을 조회해서 **500 폭발** |
| **멱등성** | LB나 클라이언트가 재시도할 때 **중복 처리** → [idempotency.md](./idempotency.md) |
| **롤백 가능한 이미지 태그** | `latest`면 되돌릴 대상이 없다 |

### 502가 나는 전형적인 순서 (실무 서사로 쓸 수 있다)

```
1. 파드 종료 시작 → SIGTERM 전달
2. 동시에 Endpoint에서 제거 요청 → LB까지 전파에 수 초 걸린다
3. 앱은 SIGTERM을 받고 즉시 종료
4. 그런데 LB는 아직 그 파드로 트래픽을 보낸다
5. → 502
해결: preStop에 sleep 5~15초를 넣어 "전파될 시간"을 벌고, 그 뒤 graceful shutdown
```

**"무중단 배포 설정했는데 502가 났고 원인이 이거였습니다"는 강력한 답변 소재다.**
자세한 건 [deployment.md](./deployment.md) 5번.

## 4. DB 스키마 변경 — 확장-수축 패턴

롤링/카나리 중에는 **구 버전과 신 버전이 같은 스키마를 동시에 쓴다.**

```
[사고]  컬럼명을 name → full_name 으로 바꾸며 배포
        → 롤링 중 구 버전 파드가 name을 조회 → 500
        → 코드는 롤백해도 스키마는 이미 바뀌어서 복구 불가

[해결: Expand and Contract]
1단계 (확장)  full_name 컬럼 추가. name은 그대로 둔다        ← 배포 1
2단계 (이중 쓰기) 코드가 두 컬럼에 모두 쓴다                   ← 배포 2
3단계 (백필)  기존 데이터를 full_name으로 복사
4단계 (읽기 전환) 읽기를 full_name으로 바꾼다                  ← 배포 3
5단계 (수축)  name 컬럼 삭제                                 ← 배포 4
```

번거롭지만 **모든 단계가 개별적으로 롤백 가능하다.** 그게 목적이다.

## 5. 꼬리질문 체크리스트

- [ ] 네 가지 방식과 각각의 트레이드오프는?
- [ ] Rolling Update만 하면 무중단인가? (→ **아니다.** readiness + graceful shutdown이 필요)
- [ ] `maxSurge`와 `maxUnavailable`은 뭔가? 무중단을 위한 값은?
- [ ] Blue/Green의 단점은? (→ 리소스 2배, **DB는 여전히 하나**)
- [ ] Blue/Green과 Canary 중 뭘 고르나? (→ 롤백 즉시성 vs 점진적 위험 감소)
- [ ] Canary의 트래픽 분할은 어떻게 구현하나? (→ LB 가중치, 서비스 메시, 파드 수)
- [ ] Canary와 A/B 테스트의 차이는? (→ 안전장치 vs 제품 실험)
- [ ] 롤링 중 두 버전이 같은 DB를 쓰는데 스키마 변경은? (→ 확장-수축)
- [ ] 무중단 배포인데 502가 나는 이유는? (→ 엔드포인트 전파 지연, preStop 부재)
- [ ] 롤백은 어떻게 하나? (→ 이전 이미지 태그 재배포, `kubectl rollout undo`, Blue/Green 스위치 복귀)
- [ ] 배포 후 무엇을 확인하나? (→ 골든 시그널 4개: 지연, 트래픽, 에러, 포화도)

## 6. 자주 하는 실수

- 방식 4개만 나열하고 끝낸다 → **전제 조건(헬스체크, graceful shutdown, 스키마 호환)이 핵심이다**
- "롤링 업데이트로 합니다"로 답한다 → 그것만으로는 무중단이 아니다
- DB 마이그레이션을 언급하지 않는다 → 배포에서 가장 위험한 부분인데 대부분 빼먹는다
- 롤백 이야기를 안 한다 → 배포를 아는 사람은 항상 롤백을 먼저 생각한다

---

# 7. DB Read Replica와 샤딩의 차이

## 0. 30초 답변

> "확장하는 **축이 다릅니다.**
> Read Replica는 **같은 데이터를 복제**해서 읽기를 여러 노드로 분산합니다. 읽기만 확장되고 쓰기는 여전히 Primary 한 대가 다 받습니다. 대신 도입이 쉽고 애플리케이션 변경이 거의 없습니다. 단점은 **복제 지연**입니다.
> 샤딩은 **데이터를 쪼개서** 여러 노드에 나눠 담습니다. 쓰기와 저장 용량까지 확장되지만, 샤드 키를 정해야 하고 **여러 샤드에 걸친 조인·트랜잭션·집계·유니크 제약이 어려워집니다.**
> 그래서 순서가 있습니다. 인덱스·쿼리 튜닝 → 캐시 → Scale-up → Read Replica → 그래도 안 되면 샤딩입니다. **샤딩은 마지막 수단**입니다."

**"축이 다르다"와 "샤딩은 마지막 수단"이 이 질문의 두 축이다.**

## 1. 그림으로 보는 차이

```
[Read Replica — 같은 데이터를 복사한다]

            쓰기 ──▶ [ Primary ]  전체 데이터
                          │ 비동기 복제 (binlog)
              ┌───────────┼───────────┐
              ▼           ▼           ▼
        [Replica1]  [Replica2]  [Replica3]   ← 전부 같은 전체 데이터
              ▲           ▲           ▲
              └─────── 읽기 분산 ──────┘

[샤딩 — 데이터를 쪼갠다]

        라우팅 (샤드 키 = user_id)
        ┌──────────┬──────────┬──────────┐
        ▼          ▼          ▼          ▼
    [Shard 0]  [Shard 1]  [Shard 2]  [Shard 3]
    user 0~999  1000~1999  2000~2999  3000~3999
    ← 각 샤드는 전체의 일부만 갖는다. 쓰기도 나뉜다
```

| | Read Replica | 샤딩 |
|---|---|---|
| 확장 대상 | **읽기** | **쓰기 + 읽기 + 저장 용량** |
| 데이터 배치 | 모든 노드가 **전체 데이터** | 노드마다 **일부 데이터** |
| 앱 변경 | 읽기 라우팅만 (작다) | **샤드 키 결정, 라우팅 계층 필요** (크다) |
| 도입 난이도 | 낮다 (RDS는 클릭 몇 번) | **매우 높다** |
| 주요 문제 | **복제 지연** | 조인·트랜잭션·집계·리샤딩 |
| 장애 시 | Replica 죽어도 서비스 가능 | **샤드 하나 죽으면 그 범위 사용자만 장애** |
| 데이터 총량 | 노드 수만큼 **중복 저장** | 나눠 저장 |
| 되돌리기 | 쉽다 (Replica 삭제) | **매우 어렵다** |

## 2. Read Replica의 핵심 문제: 복제 지연

### 왜 지연이 생기나

MySQL 복제는 기본적으로 **비동기**다.

```
1. Primary가 커밋하고 binlog에 쓴다
2. Primary는 여기서 클라이언트에 "성공"을 응답한다   ← 여기서 이미 끝
3. Replica의 I/O 스레드가 binlog를 받아 relay log에 쓴다
4. Replica의 SQL 스레드가 relay log를 실행한다      ← 여기까지 시간이 걸린다
```

지연이 커지는 원인
- **대량 UPDATE/DELETE 한 방** (예: `UPDATE ... WHERE` 로 100만 행)
- 긴 트랜잭션, 큰 DDL
- Replica의 사양이 Primary보다 낮음
- MySQL 5.x의 **단일 스레드 적용**이 병목 (8.0은 병렬 워커 지원)

### Read-after-write 문제 — 실무에서 가장 자주 겪는다

```
사용자: 프로필 이름을 "홍길동"으로 수정  → Primary에 쓰기 성공
      → 즉시 프로필 페이지 리다이렉트    → Replica에서 읽기
      → 예전 이름이 보인다              → "저장이 안 됐나요?" 문의
```

**대응 방법 (하나만 알아도 되지만 이유를 말할 수 있어야 한다)**

| 방법 | 설명 |
|---|---|
| **중요 읽기는 Primary로** | 결제, 잔액, 방금 쓴 데이터 조회는 Primary에서. 가장 흔한 해법 |
| **쓰기 후 N초 Primary 고정** | 세션에 쓰기 시각을 기록하고 일정 시간 동안 그 사용자만 Primary로 |
| **응답에 결과를 담아준다** | 쓰기 API가 갱신된 객체를 반환해서 재조회를 없앤다. 가장 깔끔하다 |
| **GTID / 위치 기반 대기** | 특정 트랜잭션이 Replica에 적용될 때까지 대기 (`WAIT_FOR_EXECUTED_GTID_SET`) |
| **준동기 복제** | 최소 1개 Replica가 받았음을 확인 후 커밋. 지연은 줄지만 **쓰기 성능이 떨어진다** |

> **"복제 지연을 어떻게 다루나"에 답할 수 있으면 이 질문은 통과다.**
> Read Replica의 존재는 누구나 알지만 지연 대응은 실제로 부딪혀본 사람만 말한다.

### 읽기 라우팅을 어디서 하나

| 방식 | 설명 |
|---|---|
| 애플리케이션 | DB 연결을 2개(writer/reader) 두고 코드에서 선택. 가장 명시적 |
| **Aurora Reader Endpoint** | 여러 Replica로 자동 분산해주는 엔드포인트 |
| RDS Proxy / ProxySQL | 프록시가 쿼리를 보고 자동 분기. 앱 변경 최소 |
| ORM 설정 | 프레임워크 수준 지원 |

**주의**: `SELECT` 라고 무조건 Replica로 보내면 안 된다. **트랜잭션 안의 `SELECT`는 Primary로 가야 한다.**

## 3. 샤딩

### 샤드 키 선택이 전부다

| 방식 | 설명 | 문제 |
|---|---|---|
| **레인지** | `user_id 0~999` → 샤드 0 | 신규 사용자가 마지막 샤드로 쏠린다 (**핫스팟**) |
| **해시** | `hash(user_id) % N` | 균등하지만 **N이 바뀌면 거의 전부 재배치** |
| **일관된 해싱** | 해시 링에 노드 배치 | 노드 추가 시 이동량이 적다 |
| **디렉터리** | 매핑 테이블을 따로 관리 | 유연하지만 **매핑 테이블이 SPOF/병목** |

**실무 정석: 논리 샤드를 물리 샤드보다 많이 만든다.**
예를 들어 논리 샤드 1024개를 만들고 물리 노드 4대에 256개씩 배치한다.
나중에 노드를 8대로 늘릴 때 **논리 샤드 단위로 옮기면 되므로 해시 재계산이 필요 없다.**
이걸 말하면 확실히 눈에 띈다.

### 샤딩하면 잃는 것

| 잃는 것 | 이유 / 대안 |
|---|---|
| **크로스 샤드 조인** | 불가능. 앱에서 조회 후 합치거나, 데이터를 비정규화해서 같은 샤드에 둔다 |
| **크로스 샤드 트랜잭션** | ACID가 깨진다. Saga 패턴이나 **결과적 일관성**을 받아들인다 |
| **전역 유니크 제약** | 이메일 유니크를 DB가 보장 못 한다. 별도 유니크 인덱스 테이블이나 앱 검증 |
| **전역 AUTO_INCREMENT** | Snowflake ID, UUID v7, ULID 같은 분산 ID 생성기가 필요 |
| **전체 집계/정렬** | `ORDER BY ... LIMIT`이 어렵다. 모든 샤드에서 가져와 앱에서 병합 |
| **페이지네이션** | 특히 어렵다. 커서 기반으로 바꾸고 샤드별 커서를 관리 |
| **스키마 변경** | 모든 샤드에 적용해야 한다 |
| **운영 복잡도** | 백업, 모니터링, 장애 대응이 샤드 수만큼 늘어난다 |

### 파티셔닝과 샤딩의 차이 (자주 묻는다)

| | 파티셔닝 | 샤딩 |
|---|---|---|
| 범위 | **하나의 DB 서버 안**에서 테이블을 물리적으로 분할 | **여러 DB 서버**로 분산 |
| 목적 | 큰 테이블 관리, 오래된 파티션 일괄 삭제, 파티션 프루닝 | 확장성 (쓰기/용량) |
| 확장 | 서버 자원은 그대로 | 서버가 늘어난다 |

**"샤딩은 수평 분할을 여러 서버로 확장한 것"** 이라고 정리하면 깔끔하다.

## 4. 그래서 순서 (가장 중요한 답)

```
1) 인덱스 / 쿼리 튜닝        ← 대부분 여기서 끝난다. 가장 싸고 효과가 크다
2) N+1 제거, 커넥션 풀 조정
3) 캐시 (Redis, CDN, 애플리케이션 캐시)
4) Scale-up
5) Read Replica (읽기 분리)
6) 아키텍처 변경 — 비동기화(SQS), CQRS, 검색은 Elasticsearch로
7) 테이블 파티셔닝, 오래된 데이터 아카이빙
8) 샤딩                     ← 마지막. 되돌리기가 가장 어렵다
```

> "저희 규모에서는 5번까지로 대부분 해결된다고 보고, 샤딩은 데이터 증가율과 쓰기 부하를 근거로 마지막에 판단할 문제라고 생각합니다."
> **이 톤이 실무 판단력으로 읽힌다.** 샤딩을 바로 꺼내는 답변보다 훨씬 낫다.

읽기 부하가 문제라면 **캐시가 Read Replica보다 먼저**인 경우도 많다. 그것도 말해두면 좋다.

## 5. 꼬리질문 체크리스트

- [ ] Read Replica와 샤딩의 차이를 한 줄로? (→ 같은 데이터 복사(읽기) vs 데이터 분할(쓰기))
- [ ] Read Replica로 쓰기 부하가 줄어드나? (→ **아니다.** 쓰기는 Primary 한 대)
- [ ] 복제 지연이 왜 생기나? 어떻게 대응하나?
- [ ] Read-after-write 문제를 겪으면 어떻게 해결하나?
- [ ] 모든 `SELECT`를 Replica로 보내도 되나? (→ 트랜잭션 내부, 방금 쓴 데이터는 Primary)
- [ ] Primary가 죽으면 어떻게 되나? (→ 페일오버. Multi-AZ, 승격 후 DNS 전환. 그 사이 쓰기 불가)
- [ ] 샤드 키는 어떻게 정하나? 잘못 정하면? (→ 핫스팟, 크로스 샤드 쿼리 폭증)
- [ ] 샤딩 후 조인은 어떻게 하나?
- [ ] 샤드를 추가할 때 무슨 일이 생기나? (→ 리샤딩. 그래서 일관된 해싱/논리 샤드)
- [ ] 파티셔닝과 샤딩의 차이는?
- [ ] 샤딩 전에 해볼 수 있는 건? (→ 위 순서 1~7)
- [ ] 샤딩 환경에서 PK는 어떻게 만드나? (→ Snowflake, UUID v7, ULID)

## 6. 자주 하는 실수

- 둘을 "DB 확장 방법"으로 뭉쳐서 말한다 → **읽기 확장 vs 쓰기 확장**이 핵심 구분이다
- Read Replica의 복제 지연을 언급하지 않는다 → 실무에서 가장 자주 부딪히는 문제다
- 샤딩을 쉽게 말한다 → **잃는 것**을 말할 수 있어야 한다
- 순서 없이 바로 샤딩을 답한다 → 판단력이 없어 보인다

---

# 8. Forward Proxy와 Reverse Proxy 차이점

> 이 주제는 [proxy.md](./proxy.md)에 상세히 정리되어 있다. 여기서는 30초 답변과 체크리스트만 둔다.
> 시각화: [visualize/proxy.html](./visualize/proxy.html)

## 0. 30초 답변

> "둘 다 클라이언트와 서버 사이의 중개자인데, **누구를 대신하고 누구를 숨기는지**가 다릅니다.
> Forward Proxy는 **클라이언트 편**에 서서 클라이언트를 대신하고, 서버에게 클라이언트를 숨깁니다. 클라이언트가 직접 설정합니다. 사내 프록시나 접근 통제, 캐싱에 씁니다.
> Reverse Proxy는 **서버 편**에 서서 서버를 대신하고, 클라이언트에게 실제 서버를 숨깁니다. 서버 운영자가 설정합니다. Nginx, ALB, CloudFront가 여기에 해당하고 **로드 밸런싱·TLS 종료·캐싱·L7 라우팅·오리진 은닉**을 줍니다.
> 헷갈리면 **'숨겨지는 쪽이 누구냐'** 로 되짚습니다."

## 1. 핵심 표

| | Forward Proxy | Reverse Proxy |
|---|---|---|
| 위치 | 클라이언트 앞 | 서버 앞 |
| 누가 설정 | 클라이언트 / 네트워크 관리자 | **서버 운영자** |
| **숨기는 대상** | **클라이언트** | **서버** |
| 클라이언트 인지 | 안다 (설정해야 하므로) | 모른다 |
| 대표 | Squid, 사내 프록시, mitmproxy | Nginx, ALB, CloudFront, Envoy |
| 목적 | 접근 통제, 익명성, 캐싱 | 로드 밸런싱, TLS 종료, 오리진 은닉 |

## 2. 꼬리질문 체크리스트

- [ ] 숨겨지는 쪽이 각각 누구인가?
- [ ] NAT Gateway는 Forward Proxy인가? (→ 엄밀히 아니다. NAT는 L3, Forward Proxy는 L7)
- [ ] CDN은 어느 쪽인가? (→ **Reverse Proxy**. 오리진을 대신한다)
- [ ] Reverse Proxy와 로드 밸런서는 같은 것인가? (→ 로드 밸런싱은 여러 기능 중 하나)
- [ ] `X-Forwarded-For`는 신뢰할 수 있나? (→ **위조 가능**. 오른쪽에서 신뢰 프록시 수만큼 세어 들어간다)
- [ ] 프록시 뒤에서 리다이렉트가 http로 나가는 이유는? (→ TLS 종료. `X-Forwarded-Proto` 확인)
- [ ] 타임아웃이 두 겹이라는 게 무슨 뜻인가? (→ 짧은 쪽이 먼저 끊는다. 504)
- [ ] 헬스 체크가 401이면? (→ 모든 파드가 unhealthy. 인증 미들웨어에서 제외)

---

# 9. 세션 기반 인증과 JWT 인증 — Scale-out 관점 비교

## 0. 30초 답변

> "Scale-out 관점에서 갈리는 건 **상태를 어디에 두는가**입니다.
> 세션 방식은 서버가 세션 저장소를 갖고 있어야 합니다. 이걸 **서버 메모리에 두면 Scale-out이 깨집니다.** 다른 서버로 요청이 가면 로그인이 풀리고, 그래서 sticky session을 걸어야 하는데 그러면 로드 밸런싱이 불균형해지고 파드가 재시작되면 세션이 날아갑니다.
> 해결은 **세션 저장소를 Redis 같은 외부로 빼는 것**입니다. 이러면 애플리케이션은 stateless가 되고 Scale-out이 가능합니다. 다만 매 요청마다 Redis 조회가 들어가고 Redis가 중요 의존성이 됩니다.
> JWT는 **토큰 자체에 정보와 서명이 들어 있어서 서버가 저장소 조회 없이 검증만** 하면 됩니다. 완전히 stateless라 Scale-out과 MSA에 유리합니다.
> 대신 **발급한 토큰을 즉시 무효화할 수 없습니다.** 강제 로그아웃, 권한 변경 즉시 반영, 토큰 탈취 대응이 어렵습니다. 그래서 실무에서는 access token을 짧게 두고 refresh token은 서버에 저장하는 **하이브리드**가 많습니다."

**마지막 문장(하이브리드)까지 가면 이 질문은 확실히 통과다.**

## 1. Scale-out 관점 비교

| | 세션 (서버 메모리) | 세션 (Redis 등 외부) | JWT |
|---|---|---|---|
| 상태 위치 | **각 서버 메모리** | 공유 저장소 | **토큰 자체 (클라이언트)** |
| Scale-out | ❌ sticky session 필요 | ✅ 가능 | ✅ 가능 |
| 앱 stateless | ❌ | ✅ | ✅ |
| 요청당 저장소 조회 | 없음 (메모리) | **있다** (네트워크 홉) | **없다** |
| 서버 재시작 | **세션 소실 → 전원 로그아웃** | 유지 | 영향 없음 |
| 즉시 무효화 | ✅ 쉽다 | ✅ 쉽다 | ❌ **어렵다** |
| 저장 부담 | 서버 메모리 | Redis 메모리 | **없다 (클라이언트가 보관)** |
| 요청 크기 | 쿠키에 세션 ID만 (작다) | 작다 | **토큰 전체 (수백 바이트~)** |
| 새 의존성 | 없음 | **Redis (가용성 중요)** | 없음 |
| 서비스 간 공유 | 저장소를 공유해야 함 | 저장소 공유 | **공개키만 있으면 각자 검증** |

## 2. 서버 메모리 세션이 Scale-out에서 깨지는 과정

```
[파드 1대]
  로그인 → 파드A 메모리에 세션 저장 → 이후 요청도 파드A → 정상

[파드 3대로 Scale-out]
  로그인 → 파드A에 세션 저장
  다음 요청 → 로드 밸런서가 파드B로 보냄 → 파드B에는 세션이 없다 → 401 로그아웃
```

### 임시 처방과 그 대가

| 처방 | 대가 |
|---|---|
| **Sticky Session** (쿠키/IP 해시로 같은 서버 고정) | 부하가 불균등해진다. **그 서버가 죽으면 그 사용자들만 로그아웃**. 배포할 때마다 세션이 날아간다. Scale-out의 이점을 반쯤 포기하는 것 |
| **세션 클러스터링** (서버 간 세션 복제) | 서버 수의 제곱으로 복제 트래픽이 늘어난다. 확장에 역행한다 |
| **Redis 세션 스토어** | ✅ **정석.** 앱이 stateless가 된다. 대가는 네트워크 홉 하나와 Redis 의존성 |

> "세션 방식이 Scale-out에 불리한 게 아니라, **세션을 서버 메모리에 두는 것**이 불리한 겁니다.
> 저장소를 밖으로 빼면 세션도 충분히 확장됩니다."
> **이 교정이 이 질문의 핵심이다.** "세션은 확장에 불리하고 JWT가 좋다"는 흔한 오해다.

## 3. JWT의 구조와 반드시 알아야 하는 사실

```
eyJhbGciOiJIUzI1NiJ9 . eyJzdWIiOiIxMjMiLCJleHAiOjE3...} . SflKxwRJSMeKKF2QT4
└──── Header ────┘   └──────── Payload ────────┘   └──── Signature ────┘
      알고리즘              사용자 정보, 만료               위조 방지 서명
```

### **JWT는 암호화가 아니라 서명이다** — 반드시 알아야 한다

Payload는 **Base64URL 인코딩일 뿐**이다. 누구나 디코딩해서 읽을 수 있다.
서명은 "내용이 바뀌지 않았음"만 보장한다.

**그래서 JWT에 비밀번호, 주민번호, 민감 개인정보를 넣으면 안 된다.**
이 한 줄을 말하면 보안 감각이 있는 것으로 읽힌다.

### 즉시 무효화 문제 — JWT의 근본 한계

```
사용자를 강제 로그아웃시키고 싶다
  → 세션: 저장소에서 세션을 지운다. 끝
  → JWT:  서버는 토큰을 갖고 있지 않다. 만료 전까지 계속 유효하다  ← 문제

관리자가 사용자 권한을 박탈했다
  → JWT 안의 role은 발급 시점 값이다. 만료까지 예전 권한으로 통과한다
```

**해결책과 그 아이러니**

| 방법 | 문제 |
|---|---|
| Access Token을 짧게 (5~15분) | 그 시간 동안은 여전히 유효하다. 완벽하진 않지만 현실적인 타협 |
| **블랙리스트를 Redis에** | ✅ 동작한다. 그런데 **매 요청마다 Redis를 조회하게 되어 stateless의 장점이 사라진다** |
| 토큰 버전 / `token_version` 컬럼 | DB 조회가 생긴다. 같은 아이러니 |

> "JWT의 stateless 장점은 즉시 무효화 요구와 근본적으로 충돌합니다.
> 그래서 실무에서는 access token은 짧게 두고, refresh token만 서버에 저장하는 하이브리드로 갑니다."
> **이 트레이드오프를 말하는 게 이 질문의 핵심이다.**

### 실무 표준 구성 (하이브리드)

```
Access Token   JWT, 만료 5~15분, 저장소 조회 없이 검증        → stateless, 빠르다
Refresh Token  불투명 토큰, 만료 2주, DB/Redis에 저장          → 즉시 폐기 가능
               HttpOnly + Secure + SameSite 쿠키에 담는다
               Rotation: 쓸 때마다 새로 발급하고 이전 것 폐기
               재사용 감지: 폐기된 refresh가 다시 오면 탈취로 보고 계열 전체 폐기
```

**"결국 상태는 어딘가에 있어야 한다. 어느 요청 경로에 둘지의 선택이다."**
Access Token 검증 경로에서는 상태를 없애고, Refresh 경로에만 상태를 둔다.
이게 하이브리드의 논리다.

### 토큰 저장 위치 (자주 이어지는 질문)

| 위치 | 위험 |
|---|---|
| localStorage | **XSS에 취약.** 스크립트가 읽어서 유출할 수 있다 |
| **HttpOnly 쿠키** | XSS로 못 읽는다. 대신 **CSRF 대비**가 필요 (`SameSite=Lax/Strict`) |
| 메모리(변수) | 가장 안전하지만 새로고침에 사라진다. refresh와 조합해서 쓴다 |

**정석: access token은 메모리, refresh token은 HttpOnly + Secure + SameSite 쿠키.**

### JWT 검증에서 놓치면 안 되는 것

- `alg: none` 공격 — 알고리즘을 **서버가 지정한 것으로 고정**하고 토큰의 헤더를 믿지 않는다
- HS256/RS256 confusion — 공개키를 HMAC 비밀키로 쓰게 만드는 공격
- `exp`, `iat`, `iss`, `aud` **검증을 반드시 한다.** 라이브러리 기본값을 확인한다
- 서명 검증 없이 디코드만 하는 함수(`decode`)를 인증에 쓰지 않는다

## 4. 그래서 무엇을 고르나 (판단 기준)

| 상황 | 선택 |
|---|---|
| 일반 웹 서비스, 강제 로그아웃/권한 즉시 반영이 중요 | **Redis 세션** 또는 하이브리드 |
| 서버가 여러 종류이고 각자 독립 검증해야 함 (MSA) | **JWT** (공개키 검증) |
| 모바일 앱 / 서드파티 API / OAuth | **JWT + Refresh** |
| 금융, 결제처럼 즉시 차단이 필수 | **세션 또는 불투명 토큰** (서버 상태 필수) |
| 트래픽이 매우 크고 인증 조회가 병목 | JWT로 조회 제거 |

> "Redis 세션도 stateless 애플리케이션을 만들 수 있으므로 Scale-out에 문제가 없습니다.
> JWT는 **저장소 조회를 없앤다는 성능/독립성 이점**과 **즉시 무효화를 잃는 대가**를 교환하는 선택입니다."

## 5. 꼬리질문 체크리스트

- [ ] 세션 방식이 Scale-out에서 왜 문제가 되나? (→ **서버 메모리**에 둘 때)
- [ ] Sticky Session의 단점은? (→ 불균등 분산, 서버 죽으면 로그아웃, 배포 시 세션 소실)
- [ ] Redis 세션의 단점은? (→ 네트워크 홉, Redis 가용성이 서비스 가용성이 된다)
- [ ] JWT는 왜 stateless인가? (→ 서명 검증만으로 완결. 저장소 조회 없음)
- [ ] **JWT를 강제 로그아웃하려면?** (→ 블랙리스트. 그러면 stateless가 깨진다)
- [ ] JWT의 payload는 암호화되어 있나? (→ **아니다.** Base64 인코딩. 민감정보 금지)
- [ ] Access/Refresh를 나누는 이유는?
- [ ] Refresh Token은 어디에 저장하나? Rotation은 왜 하나?
- [ ] JWT를 localStorage에 저장하면? (→ XSS 위험. HttpOnly 쿠키 권장)
- [ ] `alg: none` 공격이 뭔가?
- [ ] 권한이 변경되면 JWT에는 언제 반영되나? (→ 재발급 시점. 그래서 만료를 짧게)
- [ ] Redis가 죽으면 세션 방식은? (→ 전원 로그아웃. 그래서 Redis도 HA 구성)

## 6. 자주 하는 실수

- **"세션은 Scale-out에 불리하고 JWT가 좋다"고 단정한다** → Redis 세션이 정석이다. 이 오해를 교정하는 게 가장 큰 가산점
- JWT의 즉시 무효화 문제를 모른다 → JWT를 실제로 써본 적 없다는 신호로 읽힌다
- "JWT는 암호화되어 있다"고 말한다 → **서명이다.** 치명적인 오답
- 하이브리드 구성(짧은 access + 저장된 refresh)을 모른다 → 실무 표준이다
- 저장 위치(XSS/CSRF) 이야기를 못 한다 → 인증은 보안 질문으로 이어진다

---

## 마지막으로: 인프라 질문 통합 답변 카드

면접에서 인프라 질문이 나오면 **어느 질문이든 이 흐름 안에 위치시켜서 답한다.**

> "트래픽이 늘면 먼저 병목을 측정하고, 인덱스와 캐시로 해결되는지 봅니다.
> 안 되면 Scale-up으로 시간을 벌고, 그다음 Scale-out을 합니다.
> Scale-out을 하려면 앞에 **로드 밸런서**가 필요하고(L4/L7 선택), 애플리케이션이 **stateless**여야 합니다.
> 그래서 세션을 Redis로 빼거나 JWT를 쓰고, 파일은 S3로, 로그는 stdout으로 보냅니다.
> 서버를 똑같이 여러 개 찍어내려면 **컨테이너**가 필요하고, 그것들을 **VPC의 Private Subnet**에 두고 앞단에만 ALB를 노출합니다.
> 교체는 **롤링/블루그린/카나리**로 하는데, 헬스 체크와 graceful shutdown, 하위 호환 스키마가 없으면 무중단이 아닙니다.
> 앱을 늘리면 병목은 결국 DB로 내려갑니다. 읽기는 **Read Replica**로 분산하고 복제 지연을 다루고, 쓰기가 한계면 마지막에 **샤딩**을 검토합니다."

**이 문단 하나를 소리 내어 외워두면 인프라 파트 전체가 하나의 답변이 된다.**
